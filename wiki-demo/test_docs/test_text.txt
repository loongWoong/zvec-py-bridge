SurrealDB 是一个多模型数据库，同时支持文档、图和全文检索。

它使用 SurrealQL 作为查询语言，这是一种类似 SQL 但更强大的查询语言。

SurrealDB 的核心特性包括：

1. 多模型支持：文档存储、图数据库、键值存储
2. 实时查询：支持 Live Query，数据变更时自动推送
3. 权限系统：细粒度的行级和字段级权限控制
4. 嵌入式运行：可作为嵌入式数据库运行，无需单独部署服务

SurrealDB 广泛应用于 IoT、实时分析、知识图谱等场景。

它的图查询能力特别适合构建知识图谱和语义搜索系统，这正是本项目的核心需求。
