# 2026-07-23

## 分析 wiki-demo 实现 vs AI推理引擎.md 需求完成度
- 结论：综合完成度约 82%。Phase 1-3 主体落地（四索引、概念定位、本体展开、Re-Rank、软闭环 Agent），Phase 4（代码AST/增量索引）未启动。
- 关键偏差（自报进度未体现）：
  1. **reranker 概念距离分失效**：`search_documents` 候选不含 `concept_ids`，`run_agent` 传的是概念名而非 ID，导致 β=0.30 恒为 0.3，本体不参与排序。修复成本极低（回填 concept_ids + 传 ID）。
  2. 5 策略检索计划只生成不执行（仅注入提示，未并行调用）。
  3. Step 9 为软闭环（prompt 自评估 + 硬约束），无结构化评估 JSON。
  4. 概念标注为关键词子串匹配（pipeline._annotate_concepts），非 LLM；seed 文档未绑定概念。
- 产出：`wiki-demo/需求完成度分析.md`。

## 完成全部问题修复（"完成全部问题修复"任务）
- 已修复 4 关键偏差 + 2 已知简化，完成度升至 ~95%：
  1. **概念距离分失效** → `wiki_runtime` 新增 `_fetch_concept_ids`/`_attach_concept_ids`，RRF 后回填候选 concept_ids；`run_agent` 传 concept_ids（ID 而非名）给 reranker。
  2. **检索计划不执行** → `llm._execute_search_plan` 真正执行本体展开+增强查询预召回；策略5[调用链]经 code_route 激活。
  3. **软闭环** → 新增 `llm.evaluate_retrieval` 结构化评估 JSON（answer|continue），与硬约束并存。
  4. **关键词概念标注** → `llm.annotate_document_concepts`（LLM 多选）优先，`pipeline._annotate_concepts` 走此路径，关键词兜底。
  5. **Phase 4 代码 AST/索引 D** → 新增零依赖 `code_analyzer.py`（正则提取函数/类/调用链/符号表）；`wiki_runtime._code_search`（按概念绑定 file_path 定位代码）；`search_documents` 新增第5路 `code_route`；入库落库 `code_symbols`。
  6. **种子绑定** → 复核 `seed.py` 已通过实体名绑定 concept_ids（原"未绑定"判断有误），无需改；`repair_concept_bindings` 补导入文档。
- 新增 API：`/api/code-search`、`/api/documents/{id}/code-symbols`、`/api/repair-bindings`、`/api/search?code=1`。
- 验证：全部文件 `py_compile` 通过；`code_analyzer` 单元自测通过（沙箱无 surrealdb/requests/yaml/uvicorn 运行时，未实跑完整服务）。
- 产出：`wiki-demo/问题修复总结.md`。

## Review 未提交代码：发现 6 处"实际使用但未闭环"数据断点
- 对未提交变更逐调用点验证（静态分析），修正"完成度 95%"为约 88%。产出：`wiki-demo/未提交代码Review_差距与实现计划.md`。
- P0 断点（约 10 行可修）：
  1. `create_document` UPSERT 的 SET 缺 `code_symbols = $code_symbols` → 参数白传，索引 D 永不落库。
  2. `search_documents` Re-Rank 路径 `candidates_for_rerank` 字面量缺 `concept_ids`/`updated_at` 键 → `_attach_concept_ids` 白调，概念距离因子（β=0.30）与新鲜度因子（δ）仍恒为中性，偏差①修复未闭环。
  3. `pipeline._update_document_concepts` 用去前缀裸 key 拼 `UPDATE {doc_id}` → SurrealDB 解析为表名静默失败（except 吞掉），document.concept_ids 写不进；bind_concept 有规范化故 concept_binding 边正常（_code_search 不受影响）。此系潜伏既有 bug，被 repair_concept_bindings 继承放大（updated 计数虚高、永不收敛）。
- P1 断点：降级路径 results 未输出 concept_ids；`reranker.Candidate.to_dict()` 缺 concept_ids/function_name/updated_at → run_agent 二次 rerank 概念因子再断；`_structural_score` 参考集取自候选自身（应取自目标概念绑定文件）→ 结构因子有分无区分度。
- 实现计划：P0 三项先行（10 行）→ P1 三项（20 行）→ 运行时端到端验证（/api/ask 看 concept_score 区分、repair-bindings 跑两遍看收敛）。

## 全量功能差距分析（AI推理引擎.md 10 Steps + Phase 4 + 风险对策）
- 结论：wiki-demo 未实现全部功能，全量口径完成度约 78%。产出：`wiki-demo/AI推理引擎_全量差距分析与完善计划.md`。
- 三类差距：断点型 6 处（上轮已查）、缺失型 11 项、降级型 5 项。
- 新确认的缺失：PDF/HTML/DB-Schema 解析、chunk 级概念标注（chunker 无任何 concept 字段）、chunk 间关系、策略3 结构化查询、矛盾验证 verify 分支（decision 仅 answer/continue）、低置信度反问、constraints 无人消费、检索缓存、Git 增量索引、本体定期维护/review 流/版本化。
- 降级项：ontology_path 仅为步骤摘要（非关系边遍历路径）、confidence 为启发式（0.5+0.1×概念数）、无 uncertainties 输出、grep 仅限绑定文件。
- 完善计划分 5 Wave：W0 P0 断点（0.5天）→ W1 数据链（1天）→ W2 在线推理（2-3天：verify分支/真实推理路径/不确定说明/反问/constraints/缓存）→ W3 离线索引（3-5天：chunk级标注/PDF/HTML/表格/AST反哺本体/跨库grep）→ W4 Phase4 自动化（5-8天）。W0-3 完成 ≈95%，W4 完成 100%。

## 实施 W0+W1（6 断点修复，已落地）
- 已动手修复两报告中的 P0+P1 共 6 项数据断点，未提交（待 git 提交）：
  - P0-1 wiki_runtime.create_document UPSERT 补 `code_symbols = $code_symbols` → 索引 D 落库闭合。
  - P0-3 pipeline._update_document_concepts 规范化 `document:{id}` 并支持空列表清空 → document.concept_ids 写入生效，repair-bindings 收敛。
  - P0-2 重构 `_fetch_concept_ids`→`_fetch_doc_meta`（同时取 updated_at）；`candidates_for_rerank` 透传 concept_ids+updated_at；reranker 读取 → 概念距离因子(β)与新鲜度因子(δ)真正生效。
  - P1-4 降级路径 results 输出 concept_ids；P1-5 Candidate.to_dict 输出 concept_ids/function_name/updated_at → run_agent 二次 rerank 不再丢字段；P1-6 结构因子参考集改从目标概念 get_bindings 收集（区分度提升）。
- 改动文件：wiki_runtime.py、pipeline.py、reranker.py。`python -m py_compile` 通过；14 项数据链一致性静态校验全部 PASS；无旧函数名残留引用。
- 注意：W2-W4（缺失型/降级型功能）尚未实现，属更独立的功能开发，需另行排期。

## 实施 W2+W3+W4（全量功能补全，已落地未提交）
- 按 `AI推理引擎_全量差距分析与完善计划.md` 的 Wave 2/3/4 落地代码级功能，全部未提交待 git 提交。
- 改动文件：llm.py、wiki_runtime.py、concept_locator.py、chunker.py、pipeline.py、ontology.py、db.py（reranker.py 上轮已改）。
- W2（在线推理 6 项）：verify 分支（EVAL prompt + run_agent 定向核查）、真实推理路径 `_build_reasoning_path`（基于 expand_concepts 关系边，替代步骤摘要）、uncertainties（`<<UNCERTAINTIES>>` 围栏解析）、低置信反问（locate 加 candidates + run_agent 置 clarification_needed，不阻塞）、constraints 消费（注入检索提示+评估）、检索缓存（`_SEARCH_CACHE` TTL 300s）。
- W3（离线索引）：chunk 级概念标注 `_annotate_chunk_concepts`（写入 chunk metadata→zvec fields）、PDF 解析（pdfplumber 懒加载）、HTML 解析（`_split_html` 纯正则）、MD 表格独立 chunk（`_is_markdown_table`+type=table）、超长子块 parent_summary、AST→本体草案 `propose_ontology_from_directory`、跨库 grep `grep_documents` 纳入 `_execute_search_plan`。
- W4（Phase4）：db 加 `concept.status`/`document.commit_hash`；ontology 加 `create_concept(status)`/`list_concepts(status)`/`snapshot_concepts_versions`/`rollback_concepts`/`set_concept_status`/`get_pending_review`；concept_locator 仅用 approved 概念；wiki_runtime 加 `query_log_analysis`/`reindex_changed_files`/`list_documents_by_commit`/`_filter_since_commit`(search since_commit)、create_document 加 commit_hash。
- 验证：全模块 `py_compile` 通过；38 项「定义+调用」静态一致性校验全部 PASS；纯逻辑函数（_split_uncertainties/_build_reasoning_path/propose_ontology_from_directory）离线单测通过。沙箱无 requests/surrealdb 故未实跑完整服务。
- 明确未做（需环境/排期）：W4.2 定期维护调度器（依赖 cron/CI，构件已就绪）、检索端真正消费 chunk 级概念、since_commit 仅精确匹配。
- 全量完成度由 ~78% 升至 ≈95%。产出：`wiki-demo/W2-W4_实施记录.md`。
- 建议：合并前在具备依赖环境跑端到端验收（/api/ask 看 uncertainties/ontology_path 推理边/verify 触发；ingest .pdf/.html 看 chunk_type；repair-bindings 收敛；query_log_analysis 出盲区 TOP-N）。

## 前端界面补全 + 后端路由暴露（W2-W4 功能"通电"收尾）
- 用户要求：继续补全剩余缺失功能，并完善对应前端界面。此前后端函数已实现但未暴露路由、前端无对应 UI。
- app.py 新增 7 个路由（FastAPI+pydantic 请求模型）：`GET /api/ontology/review/pending`、`POST /api/ontology/review`（set_concept_status）、`POST /api/ontology/snapshot`、`POST /api/ontology/rollback`、`GET /api/analytics/query-log`、`POST /api/pipeline/reindex-changed`、`GET /api/search/grep`。
- static/index.html（原生 JS+D3 单页）补全 UI：
  - 新增「分析」Tab（查询日志盲区分析 / Git 增量索引 / 本体版本管理三个卡片）。
  - 问答面板：渲染真实推理路径（ontology_path 推理边链 + 命中文档）、不确定点、低置信反问提示、置信度徽章（askExtrasHtml）。
  - 搜索：rerank 子分数徽章（S/C/R/F）、代码检索开关、正则 grep 模式（renderScoreBadges）。
  - 本体面板：概念列表 status 徽章；详情加审核动作（通过/待定/驳回）；工具栏加快照/回滚/待审核。
  - 文档详情：新增「代码符号」按钮（调 /api/documents/{id}/code-symbols，读 d.symbols）；编译上传 accept 扩到 pdf/html/代码/json/yaml。
- 验证：`py_compile` 通过；node --check 前端 JS 通过；10 个新 JS 函数全部定义、7 个新 API 引用全部命中；代码符号字段已修正为 d.symbols（路由用 **symbols 展开，symbol 无 file_path 仅有 line）。
- git 状态：本次新增改动仅 app.py(+76) 与 index.html(+279)；W0-W4 后端改动此前已提交（仓库起始工作树干净）。全部未提交待 git 提交。

## R6：补齐三项"未做"功能（W3.1 / W4.1 / W4.2）
- 用户："继续实现未做的功能"。对照 W2-W4 实施记录中"明确未做"清单，落地最后三项，全量完成度升至 ≈100%。
- **W3.1 检索端消费 chunk 级概念**（wiki_runtime + zvec_client）：
  - 新增 `_chunk_concept_ids(text, heading)`：query 时对每个命中 chunk 确定性重算其所属概念（与 `pipeline._annotate_chunk_concepts` 同逻辑，60s 缓存 concept 表）。**不依赖 zvec 落库字段**，对旧集合同样生效（零风险，不改 zvec schema 避免清空向量）。
  - `_search_vector` 按 doc_id 聚合 chunk 级概念并集；`search_documents` 的 RRF 聚合与 Re-Rank 候选构建用 chunk 级精确集合**覆盖**文档级 concept_ids 超集（概念距离因子 β 按命中段落生效）；结果透出 `matched_concept_ids`。
  - `zvec_client.search` 输出字段加 `concept_ids`（若 zvec 已存则优先用）。
- **W4.1 since_commit 区间语义**（db + pipeline + wiki_runtime）：
  - db 新增 `document.commit_ts`（epoch 秒）；`create_document` 收 `commit_ts`；`pipeline._resolve_commit_ts` 入库时 `git show -s --format=%ct` 解析 commit 时间戳绑定文档。
  - `wiki_runtime._git_commit_ts`（缓存）+ `_filter_since_commit` 由精确匹配升级为「commit_ts ≥ since_commit 时间戳」区间过滤；git 不可用时安全退化精确匹配；旧文档无 commit_ts 回退到其 commit_hash 时间戳。
- **W4.2 本体定期维护调度器**（新增 maintenance_scheduler.py + llm + app + 前端）：
  - `maintenance_scheduler.run_maintenance`：扫描近期文档（updated 近 N 天）+ `query_log_analysis` 盲区 → LLM `propose_new_concepts` 提议新概念 → `ontology.create_concept(status="proposed")` 进入 W4.5 审核流 → `snapshot_concepts_versions` + 写 `ontology_maintenance_report.json`。
  - 后台调度：`start_scheduler`/`stop_scheduler`/`scheduler_status` 用 APScheduler（可选依赖，缺失则降级手动）。LLM 不可用时盲区启发式兜底。
  - `llm.propose_new_concepts(docs, existing_names, top_n)` 新增（JSON 返回，过滤已存在）。
  - app.py 新增 `import maintenance_scheduler as ms` + 6 路由：`POST /api/maintenance/run`、`GET /api/maintenance/status`、`POST /api/maintenance/start`、`POST /api/maintenance/stop`、`GET /api/maintenance/report`、`GET /api/maintenance/proposals`。
  - 前端分析 Tab 新增「本体定期维护(W4.2)」卡片：仓库路径/扫描天数/调度间隔输入 + 立即运行/启动定时/停止 按钮 + 状态与结果展示；运行后刷新本体列表与待审核。
- 验证：`py_compile` 全改过模块通过；`node --check` 前端 JS 通过（{} () [] 平衡）；跨模块调用一致性 grep 全命中；git 时间戳解析在仓库内实跑返回 epoch（W4.1 机制可用）。沙箱仍缺 requests/surrealdb 故未实跑完整服务。
- git 状态：本轮新增/改动 wiki_runtime.py、pipeline.py、db.py、zvec_client.py、llm.py、maintenance_scheduler.py(新)、app.py、static/index.html、W2-W4_实施记录.md。全部未提交待 git 提交。
